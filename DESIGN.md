## Файловые Расширения

* **Исходный код:** `.v`
* **Байт-код:** `.vc`
* **Примечание:** Расширение `.v` конфликтует с существующими языками (Vlang, Verilog). Это может вызвать проблемы с инструментами (подсветка синтаксиса, сборка) и у пользователей. Решение принято использовать `.v` осознанно.
